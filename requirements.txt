# Core dependencies
MetaTrader5>=5.0.45
python-dotenv>=1.0.0
pydantic>=2.0.0
psutil>=5.9.0

# API & Backend
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
websockets>=11.0
requests>=2.31.0

# Data analysis and manipulation
pandas>=2.0.0
numpy>=1.24.0

# ML — signal quality filter (optional: if not installed, heuristic mode is used)
lightgbm>=4.0.0
scikit-learn>=1.3.0
certifi>=2024.0.0

# Reporte semanal con IA (optional: if not installed or no API key, fixed-format fallback is used)
anthropic>=0.40.0
